diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -11,6 +11,10 @@ set(CMAKE_CXX_VISIBILITY_PRESET "hidden")

 if(LSFGVK_ANDROID_WINE)
     add_compile_definitions(LSFGVK_ANDROID_WINE=1)
+    # Expose Android-specific Vulkan extension types
+    # (VkAndroidHardwareBufferFormatPropertiesANDROID etc.) to volk and to
+    # all subprojects.
+    add_compile_definitions(VK_USE_PLATFORM_ANDROID_KHR=1)
 endif()

 # subprojects
@@ -51,6 +55,8 @@ target_link_libraries(lsfg-vk PRIVATE

 if(ANDROID)
     target_link_libraries(lsfg-vk PRIVATE android)
+    # Newer framegen calls __android_log_print — link liblog.so explicitly.
+    target_link_libraries(lsfg-vk PRIVATE log)
     target_include_directories(lsfg-vk PRIVATE "${ANDROID_NDK}/sources/android/native_app_glue")
 endif()

